APP_NAME=Tableau de bord DPI API
APP_ENV=development

# Replace these values with the SQL table already analysed.
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
DATABASE_URL=sqlite:///./data/dashboard.db
SQL_TABLE_NAME=suivi_dpi_sites
SQL_SCHEMA=

# Comma-separated origins allowed by the API during development.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
